import React from 'react';
import { StyledDivBlock } from './styles/DivBlock.styled';

const DivBlock = ({ children }) => {
  return <StyledDivBlock>{children}</StyledDivBlock>;
};

export default DivBlock;
